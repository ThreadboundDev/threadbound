# Rejected ledge hang

`ledge_hang_rejected.png` is the previous active four-frame ledge-hang sheet.
It is retained only for visual comparison and rollback. The active player now
holds the first `Wall_Cling` frame and uses `../ledge_climb.png` for the
four-pose climb transition.

Do not restore this sheet without review: its raised arm and transition anatomy
were rejected in playtesting.
