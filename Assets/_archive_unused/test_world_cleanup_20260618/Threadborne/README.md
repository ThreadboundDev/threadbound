# Threadborne Player Assets

Current scene-facing sprite folders:

- `Idle/`
- `Run/`
- `Player_Normalized_V1/`
- `Equipment/`

Reference and source folders:

- `RigBase/`: cut rig pieces and rig reference work.
- `source_original_keyframes/`: older unnormalized jump/grapple keyframes kept for reference.

The active sprite player scene should prefer the normalized keyframes unless we intentionally replace the animation set.
